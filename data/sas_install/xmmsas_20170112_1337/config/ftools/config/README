FTOOLS parameter files
======================

These parameter files were copied from FTOOLS v4.2. The following changes
have been made:

 - some parameter types replaced to give better widgets (e.g. s -> e)

 - layout files added
   - mode parameter omitted from layout
   - some conditional parameters added (e.g. fdump prdata)

Notes
=====

